<p id="notice"><%= notice %></p>

<p>
  <strong>Movie:</strong>
  <%= @photo.movie.title %>
</p>

<p>
  <strong>Image url:</strong>
  <%= image_tag @photo.image_url.url(:medium) %>
</p>

<%= link_to 'Edit', edit_photo_path(@photo) %> |
<%= link_to 'Back', photos_path %>
